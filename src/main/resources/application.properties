server.port=8888

#mysql配置
spring.datasource.url=jdbc:mysql://47.101.221.184:3306/pet_db?serverTimezone=UTC
#spring.datasource.url=jdbc:mysql://127.0.0.1:3306/pet_db?serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=123456
spring.datasource.driver-class-name=com.mysql.jdbc.Driver

spring.jackson.date-format=yyyy-MM-dd HH:mm:ss
spring.jackson.time-zone=GMT+8

spring.mvc.throw-exception-if-no-handler-found=true
spring.resources.add-mappings=false

#mybatis mapper文件的位置
mybatis-plus.mapper-locations=classpath*:mapper/*.xml
#扫描pojo类的位置,在此处指明扫描实体类的包，在mapper中就可以不用写pojo类的全路径名了
mybatis.type-aliases-package=com.sise.pet.entity

# v1:管理端 v2:用户端
shiro.anonUrl=/api/v1/manager/login,/api/v1/manager/logout,/api/v1/pictures,/api/v2/userLogin,/api/v2/hospital/**,/api/v2/banner/**,/api/v2/article/**,/api/v2/boarding-home/**,/api/v2/pet/**,/api/v2/dict-detail/**,/images/**

#redis配置
spring.redis.host=47.101.221.184
spring.redis.port=6379
spring.redis.password=ppnn13%
spring.redis.timeout=0
spring.redis.jedis.pool.max-active=2000
spring.redis.jedis.pool.min-idle=8
spring.redis.jedis.pool.max-idle=500
spring.redis.jedis.pool.max-wait=10000

